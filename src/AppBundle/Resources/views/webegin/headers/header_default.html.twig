<nav class="navbar navbar-inverse navbar-fixed-top">
	<div class="container">
	<div class="row">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="#">webegin</a>
		</div>
		<div id="navbar" class="navbar-collapse collapse">
			<form class="navbar-form navbar-right">
				{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                	<span style="color:white">
                		{{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }}
                	</span>
                	{% if is_granted("ROLE_ADMIN") %}
                	<a href="{{ path('admin_homepage') }}" class="btn btn-success"><i class="fa fa-cog"></i> Admin</a>
                	{% endif %}
                	{{ facebook_login_button({'autologoutlink': false}) }}
	                <a href="{{ path('fos_user_security_logout') }}" class="btn btn-danger">
	                    {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
	                </a>
	            {% else %}
	            	<a href="{{ path('fos_user_security_login') }}" class="btn btn-primary">Login</a>
					<a href="{{ path('fos_user_registration_register') }}" class="btn btn-info">Register</a>
	            {% endif %}
			</form>
		</div><!--/.navbar-collapse -->
	</div>
	</div>
</nav>